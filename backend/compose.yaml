services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: commerce_operations
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce
    ports:
      - "5432:5432"
    volumes:
      - commerce-operations-postgres:/var/lib/postgresql/data

volumes:
  commerce-operations-postgres:
